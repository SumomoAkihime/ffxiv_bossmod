# 诱拐魔雷达制作记录

## 输入与身份

- 回放：`C:\Users\amule\Desktop\新建文件夹 (2)\CE213诱拐魔.log`
- SHA256：`7E4B6633125EE946BC27E888F4EA551E6D234259E966916C29BE878D2FD9999A`
- 格式：BossMod `BLCB`，记录版本 30，游戏版本 `2026.07.16.0001.0000`
- 区域：`Zone 1346`
- CFC / GroupID：`1093`
- 主怪：诱拐魔，`OID 0x4BE1`，`NameID 14505`
- 场地中心：`(-150, -860)`，半径 30
- 目标战斗：回放开始至主怪在 `333.36s` 销毁；之后事件属于其他任务，不参与本模块分析。

## 移动风圈

- 风圈实体：`OID 0x4BE2`，实际接触范围半径 4。
- 风圈沿场心做圆周运动，内环半径约 12、速度约 `1.63/s`，外环半径约 20、速度约 `3.17/s`。
- 为提供躲避预留，雷达使用半径 4、向当前移动方向延伸 4 的胶囊形区域。
- 移动方向优先取实体当前帧位移；没有位移时回退到实体朝向。

## 撕裂之风提前预测

- 图标：`Icon 506`，施加于即将发动机制的风圈。
- 图标至风圈停止并开始 `AID 47438`：15 份样本为 `4.04-4.15s`，采用 4.1 秒。
- 图标至十字实际生效：约 5.0 秒。
- 内环角速度：约 `8.45°/s`。
- 外环角速度：约 `9.9°/s`。
- 依据当前径向位置、运行方向、所在内外环和剩余运动时间预测停止点。
- 15 份样本的预测落点误差为 `0-0.29`。

每次判定由两个 `AID 47439` Helper 同时形成：

1. 以正北为基准的横竖正十字。
2. 以正北偏转 45 度的斜十字。

图标出现后立即在预测停止点显示两组十字；Helper 正式读条开始后切换为实际位置和朝向，动作生效后清除。

## 实测重点

- 快速外环风圈前方延伸 4 是否提供了足够反应时间，且不会造成过度遮挡。
- 图标阶段的十字预测位置是否与实机最终停止点一致。
- 正十字和斜十字是否始终以地图正北为基准，不随风圈自身朝向旋转。
- 本回放只覆盖一个任务样本；若后续出现不同环半径或速度，需要重新计算预测常量。
